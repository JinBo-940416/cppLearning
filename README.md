# C++ Exploration & Learning Concepts

This repository serves as a practical workspace for exploring C++ concepts, system-level programming, and performance optimizations. 

Rather than a single monolithic project, this repository is designed as a collection of isolated, highly focused experiments. 

## 📂 Repository Structure

Every folder in this repository represents a dedicated learning point or concept. 

```text
.
├── atomic_types_lock_free_checking/       # Check is_lock_free() on atomic buildin types and atomic struct types.
├── SPSC_lockfree_queue/                   # A simple single producer single consumer lock free ring.
├── std_atomic_flag/                       # Use std::atomic_flag to implement a simple lock and a lockguard.
├── coding_toto/                           # Just to let me feel how rare it is to win toto category 1 price and happily retire.
└── ...